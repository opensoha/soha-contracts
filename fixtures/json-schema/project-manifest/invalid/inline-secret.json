{
  "apiVersion": "opensoha.io/v1alpha1",
  "kind": "Project",
  "metadata": { "name": "demo" },
  "spec": {
    "steps": [
      {
        "id": "create-vm",
        "tool": "virtualization.vms.create.trigger",
        "input": { "connectionId": "pve-main", "password": "not-allowed" }
      }
    ]
  }
}

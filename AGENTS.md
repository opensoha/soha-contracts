# Soha Contracts 仓库入口

- 本仓是公开 OpenAPI、JSON Schema、生成 SDK 和兼容性基线的真实源；先改源契约再运行生成入口，不手改 `gen/` 下的 DTO。
- 在 OpenSoha 多仓工作区中读取 `../AGENTS.md` 一次；独立克隆时使用本仓规则，不要求自动初始化相邻仓库或规划工具。
- 契约实现或实质审查前读取 [soha-contracts](.agents/skills/soha-contracts/SKILL.md) 及本次相关参考，不只依赖技能自动匹配。已读且未变化的内容可复用；任务切换或关键上下文丢失时仅补读必要部分。
- 主入口为 `npm test`；公开契约改动必须执行兼容性和完整 consumer 矩阵，使用 [CI](.github/workflows/ci.yml) 与包脚本中的真实参数。缺少 consumer 或发布依赖时报告验证缺口，不用本地替换掩盖缺失。
- 文档和技能改动只检查内容、链接与差异；相关代码和环境未变化时复用成功验证，保留用户未提交改动。

## 变更与验收边界

- 修改前明确公开行为、受影响 consumer、兼容范围和验证入口。契约还包括权限、分页口径、错误、幂等性和副作用，不只包括 DTO 字段；不为 consumer 的纯视觉修改调整契约。
- 先核实 schema、实现和测试的现状，再对照有效需求；历史代码和规划不是新契约的自动依据。冲突需要说明，不能静默选择更宽松的解释。
- 按真实源和依赖方向更新 consumer，不复制业务逻辑，不把本次变更扩大为全仓 API 或生成器重构。
- 验证记录绑定 contracts 提交、consumer 实际提交或发布版本、命令和结果。固定兼容组合与跟随最新分支的集成结果分开记录；不以静默改 pin 或兼容基线换取通过。
- 已通过、失败、跳过和未运行必须分开报告；生成成功不等于兼容矩阵通过，编译通过不等于业务语义已验证。既有 CI 门禁不得因文档精简而降低。
